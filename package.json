{
  "name": "randaworks-site",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 8888 --host dev.randa",
    "build": "astro build && node scripts/clean-dist.mjs",
    "preview": "astro preview --port 8888",
    "deploy:preview": "npm run build && npx wrangler deploy --env preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.3",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.4",
    "mdast-util-to-string": "^4.0.0",
    "reading-time": "^1.5.0",
    "rehype-autolink-headings": "^7.1.0",
    "rehype-slug": "^6.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "typescript": "^5.9.3"
  }
}
